const DEFAULT_ENVIROMENT = {
  production: "production",
};

const DEFAULT_API_URL = "https://api-referral.vndigitech.com";

const DEFAULT_ADMIN_ACCOUNT = {
  userid: "admin",
  password: "12345678",
  grantType: "system_account",
};

const DEFAULT_CONFIG = {
  port: "3000",
  websiteTitle: "Referral Default Title",
};

module.exports = {
  DEFAULT_API_URL, DEFAULT_CONFIG, DEFAULT_ENVIROMENT, DEFAULT_ADMIN_ACCOUNT
};